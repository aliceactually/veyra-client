[project]
name = "veyra-client"
version = "0.10.1"
description = "A minimal terminal client for Codex App Server"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
